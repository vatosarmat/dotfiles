##### Path
- `realpath` - get absolute path
  - `realpath --relative-to=DIR` - get relative path
- `dirname`

##### getopts
Parses named assignments
But seems requires positionals to be after the named
